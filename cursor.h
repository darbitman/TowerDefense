#pragma once

#include "model.h"
#include "tile.h"

class Cursor : public Model
{
public:
  Cursor();
  //Cursor(Model*, Coord xy, char = (char)(219));

	//Model * get_cursor();
	//Model * get_underneath();

	//void set_cursor( Model * );
	//void set_underneath( Model * );

private:
  //Model *cursor;
  //Model *underneath;
};